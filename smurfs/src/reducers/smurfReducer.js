// import the actions

const initialState = {
    villiageData: [],
    isFetchingData: false,
    error: ''
}

export const smurfReducer = (state=initialState, action) => {
    switch (action.type){


        default:
            return state;
    }
}